# Development environment values for Expo AI Web
# This file overrides the base react chart values for development
 
replicaCount: 1
 
fullnameOverride: "manufacturing-execution-system-web"
 
# =============================================================================
# ENVIRONMENT VARIABLES
# =============================================================================
env:
  # Internal K8s service URL for nginx reverse proxy (service port is 80, not container port 5000)
  API_BACKEND_URL: "manufacturing-execution-system-api.manufacturing-execution-system-dev.svc.cluster.local:80"
  VITE_API_URL: "/api"
 
envSecret: {}
 
envFrom: []
 
# =============================================================================
# HTTPROUTE (Gateway API)
# =============================================================================
httpRoute:
  enabled: true
  annotations: {}
  labels: {}
  parentRefs:
    - name: https-gateway
      namespace: gateway-system
      sectionName: https-dev
  hostnames:
    - mes.dev.gcp.codespiresolutions.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: manufacturing-execution-system-web
          port: 80
      filters: []
  tls: {}
 
podLabels:
  app.kubernetes.io/environment: development
 